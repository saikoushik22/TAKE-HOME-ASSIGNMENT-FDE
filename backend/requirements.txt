# ---------- Web framework ----------
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0

# ---------- Database ----------
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
pgvector==0.3.6

# ---------- HTTP / parsing ----------
httpx==0.28.1
PyYAML==6.0.2

# ---------- Cloud LLM providers (optional at runtime) ----------
# Installed always so the provider registry can import them, but never required:
# a provider without a configured key reports itself unavailable rather than failing.
anthropic==0.42.0
openai==1.59.6
